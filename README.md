# InstagramBot
A simple instagram bot which logs into the user's account with the credentials provided and automatically likes the pictures under a certain tag, as specified by the user.
Requirements: Selenium
Note:Please download a version selenium according to the version of chrome you're using and give the path where selenium is installed in your system in the code.
